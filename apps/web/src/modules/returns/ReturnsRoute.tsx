import { AppShell } from '../../components/AppShell';
import { ReturnLookupScreen } from './ReturnLookupScreen';

export default function ReturnsRoute(): React.JSX.Element {
  return (
    <AppShell>
      <ReturnLookupScreen />
    </AppShell>
  );
}
